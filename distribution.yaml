name: hermesbro-designbro
version: 1.0.2
description: "Designer AI personale. Landing page, loghi, brand identity, deck, materiali visivi."
hermes_requires: ">=0.16.0"
author: "HermesBro"
license: "MIT"

env_requires:
  - name: TELEGRAM_BOT_TOKEN
    description: "Telegram bot token (da @BotFather)"
    required: true
  - name: DEEPSEEK_API_KEY
    description: "DeepSeek API key per LLM"
    required: true
